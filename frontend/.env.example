# Pode ser `http://localhost:3000` ou `http://localhost:3000/api` — o cliente normaliza para sempre usar `/api`.
VITE_API_URL=http://localhost:3000/api
# Limiar de comprovante obrigatório: só no backend (REIMBURSEMENT_REQUIRE_ATTACHMENT_ABOVE_VALUE). A UI usa GET /api/config/reimbursement-rules.
