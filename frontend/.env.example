# Pode ser `http://localhost:3000` ou `http://localhost:3000/api` — o cliente normaliza para sempre usar `/api`.
VITE_API_URL=http://localhost:3000/api
# Opcional: mesmo limite que REIMBURSEMENT_REQUIRE_ATTACHMENT_ABOVE_VALUE no backend (só para aviso na UI). 0 = não exibir aviso.
# VITE_REIMBURSEMENT_REQUIRE_ATTACHMENT_ABOVE_VALUE=500
